# examples/

Placeholder examples for evalgate.

- `basic_dataset.yaml` — minimal three-case smoke dataset used by CI against the mock server.

Additional dataset examples (coding, reasoning, safety) will land in a future phase.
